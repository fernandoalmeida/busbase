<!DOCTYPE html>
<html>
	<head>
		<title>Parada De Onibus Web</title>
		<%= stylesheet_link_tag :all %>
		<%= javascript_include_tag :defaults %>
		<%= csrf_meta_tag %>
	</head>
	<body>
		<div class="message error" id="flash_notice"><%= "<p>#{flash[:notice]}</p>".html_safe if flash[:notice] %></div>
		<%= yield %>
	</body>
</html>
